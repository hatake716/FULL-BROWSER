<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".App"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:extractNativeLibs="true"
        android:networkSecurityConfig="@xml/network_security"
        android:enableOnBackInvokedCallback="false"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault.NoActionBar"
        tools:ignore="MissingApplicationIcon,DataExtractionRules">

        <!-- 入口。rootfs があればセッション開始、無ければ SetupActivity へ -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|keyboard|uiMode|smallestScreenSize|density"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data android:name="android.app.shortcuts" android:resource="@xml/shortcuts" />
        </activity>

        <activity android:name=".SetupActivity" android:exported="false" />
        <activity android:name=".SettingsActivity" android:exported="false" />

        <!-- ブラウザセッション維持 (docs/PLAY-COMPLIANCE.md: specialUse の申告が必要) -->
        <service
            android:name=".SessionService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keeps a user-initiated desktop Linux browser session (proot) alive while the app is in the background, like a terminal emulator" />
        </service>

        <!-- ネイティブ X サーバ (lorie)。Xorg はプロセス全域の状態を持つため専用プロセスでホストする -->
        <service
            android:name=".XServerService"
            android:exported="false"
            android:process=":x11"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Runs a local X11 display server in a dedicated process to render the user-initiated desktop Linux browser; no standard FGS type applies (no screen capture, no external device, no data sync)" />
        </service>
    </application>
</manifest>
