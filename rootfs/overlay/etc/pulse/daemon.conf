; FULL-BROWSER: SW デコードと同居するため、大きめのバッファで途切れ耐性を優先する。
; この遅延は PulseAudio がクライアント (ブラウザ) に報告するため A/V 同期は保たれる。
default-fragments = 8
default-fragment-size-msec = 60
avoid-resampling = yes
resample-method = speex-fixed-1
